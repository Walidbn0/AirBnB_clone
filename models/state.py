#!/usr/bin/python3
from models.base_models import BaseModel

class State(BaseModel):
     """A class that represents a state

    Attribute:
       (str) name : the name of the state
    """
     name = ""
